<?php

declare(strict_types=1);

namespace Heritage\Contracts\Repositories;

/**
 * Interface RepositoryInterface
 *
 * Root marker contract for all domain and entity repositories in the Ugarit Ecosystem.
 *
 * Purpose:
 * - Provides a unified type identity for the repository layer.
 * - Allows the service container and RepositoryFactory to identify repository bindings.
 */
interface RepositoryInterface
{
    // Base marker interface
}
